{
  "name": "ipbjns-now",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "rebuild": "node -v | grep v8 && (cd src; npm run build)",
    "deploy": "test -n \"$URL\" && now -e URL=\"$URL\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Jim Pick (@jimpick)",
  "license": "MIT",
  "dependencies": {
  }
}
